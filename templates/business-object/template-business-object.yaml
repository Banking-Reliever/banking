meta:
  bcm_name: BCM Urbanisation
  version: 0.0.1
  template_version: 1.0.0
  last_updated: 'YYYY-MM-DD'
  owners:
    - Owner 1
    - Owner 2

# ──────────────────────────────────────────────────────────────
# Business objects
# ──────────────────────────────────────────────────────────────
#
# A business object is a high-level business abstraction,
# produced (emitted) by a BCM capability.
# It serves as a functional contract, independent of operational details.
#
# Identifier convention:
#   OBJ.<ZONE>.<L1>.<CODE>   e.g. OBJ.CORE.005.CLAIM_CASE
#
# ──────────────────────────────────────────────────────────────

resources:

  - id: OBJ.DOM.000.XXX                       # Unique business object identifier
    name: Business object name                # Short and explicit name
    definition: >-                            # Business definition of the business object
      Functional description of the business object:
      what it represents, its scope, and main usages.
    

    emitting_capability: CAP.CORE.005.IND     # L2 BCM capability that produces / emits this business object
                                             # Expected format: CAP.<ZONE>.<L1>.<L2>
                                             # (must match an id in capabilities-*.yaml)
    emitting_capability_L3:                   # Optional: list of L3 BCM emitting capabilities
                                             # Rule 1: this list must contain EXCLUSIVELY L3 ids
                                             # Rule 2: omit this attribute entirely if no L3 exists for the L2
                                             # Rule 3: L3 format must be explicit: CAP.<ZONE>.<L1>.<L2>.<L3>
                                             # (must match an id in capabilities-*.yaml)
      - CAP.CORE.005.IND.COV

    data:                                     # Data carried by the business object
      - name: field_1                         #   field / attribute name
        type: string                          #   type (string, number, date, boolean, object, array)
        description: Field 1 description      #   functional description
        required: true     


      - name: field_2
        type: date
        description: Field 2 description
        required: false


    # Optional fields
    adrs: []                                  # Associated ADRs
    tags: []                                  # Free tags for filtering / search
